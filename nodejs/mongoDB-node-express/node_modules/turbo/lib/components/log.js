"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const net_1 = require("@turbo/net");
function log(type, client) {
    let first = true;
    client.on("sync", state => {
        if (first) {
            first = false;
            const path = type === "turbo"
                ? state.logStream.turboSocket
                : state.logStream.targetSocket;
            const logClient = new net_1.LogClient(path);
            logClient.on("data", (msg) => {
                process.stdout.write(msg);
            });
            logClient.connect();
        }
    });
}
exports.log = log;
//# sourceMappingURL=log.js.map