"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@turbo/core");
const net_1 = require("@turbo/net");
const react_1 = __importDefault(require("react"));
const renderer_1 = require("../renderer");
const eval_1 = require("../components/eval");
function evaluate(turbo, expr) {
    const sessionId = core_1.getCurrentSessionId(turbo);
    if (!sessionId) {
        console.error("unable to identify current session");
        return;
    }
    const client = new net_1.Client(turbo, { type: "managed", sessionId });
    client.once("sync", async (state) => {
        const target = state.target;
        if (!target.paused) {
            core_1.logger.error("target is not paused");
            process.exit(1);
        }
        else {
            const topCallFrame = target.callFrames[0];
            const result = await client.eval(expr, topCallFrame.id);
            renderer_1.render(react_1.default.createElement(eval_1.Eval, { result: result }), "stdout");
            client.close();
            process.exit(0);
        }
    });
    client.connect();
}
exports.evaluate = evaluate;
//# sourceMappingURL=eval.js.map