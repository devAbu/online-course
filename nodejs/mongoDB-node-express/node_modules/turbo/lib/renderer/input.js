"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const hooks_1 = require("./hooks");
const box_1 = require("./box");
const cursor_1 = require("./cursor");
function Input(props) {
    const [before, setBefore] = react_1.default.useState("");
    const [after, setAfter] = react_1.default.useState("");
    function moveLeft() {
        if (before.length > 0) {
            const c = before[before.length - 1];
            setBefore(before.substring(0, before.length - 1));
            setAfter(c + after);
        }
    }
    function moveRight() {
        if (after.length > 0) {
            const c = after[0];
            setBefore(before + c);
            setAfter(after.substring(1));
        }
    }
    hooks_1.useInput(event => {
        if (event.name === "LEFT") {
            moveLeft();
        }
        else if (event.name === "RIGHT") {
            moveRight();
        }
        else if (event.name === "BACKSPACE") {
            setBefore(before.substring(0, before.length - 1));
        }
        else if (event.name === "DELETE") {
            setAfter(after.substring(1));
        }
        else if (event.name === "ENTER") {
            if (props.onSubmit) {
                props.onSubmit(before + after);
            }
            setBefore("");
            setAfter("");
        }
        else if (event.isChar) {
            setBefore(before + event.char);
        }
    });
    return (react_1.default.createElement(box_1.Box, { minHeight: 1, direction: "row" },
        props.prompt,
        before,
        react_1.default.createElement(cursor_1.Cursor, null),
        after));
}
exports.Input = Input;
//# sourceMappingURL=input.js.map