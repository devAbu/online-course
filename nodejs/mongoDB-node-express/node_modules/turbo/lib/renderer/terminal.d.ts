import { BufferTargetEvents, BufferTargetMode, DrawAttribute } from "./buffertarget";
import { EmitterBase } from "@turbo/core";
export declare type TerminalMouseEventType = "MOUSE_LEFT_BUTTON_PRESSED" | "MOUSE_LEFT_BUTTON_RELEASED" | "MOUSE_RIGHT_BUTTON_PRESSED" | "MOUSE_RIGHT_BUTTON_RELEASED" | "MOUSE_MIDDLE_BUTTON_PRESSED" | "MOUSE_MIDDLE_BUTTON_RELEASED" | "MOUSE_WHEEL_UP" | "MOUSE_WHEEL_DOWN" | "MOUSE_OTHER_BUTTON_PRESSED" | "MOUSE_OTHER_BUTTON_RELEASED" | "MOUSE_BUTTON_RELEASED";
export interface TerminalMouseEvent {
    x: number;
    y: number;
    ctrl: boolean;
    alt: boolean;
    shift: boolean;
}
export declare class TerminalBufferTarget extends EmitterBase<BufferTargetEvents> {
    private readonly buffer;
    private cursor;
    private mode;
    width: number;
    height: number | undefined;
    constructor(mode: BufferTargetMode);
    setup(): void;
    fillBg(xmin: number, xmax: number, ymin: number, ymax: number, color: string | number | undefined): void;
    draw(x: number, y: number, xmin: number, xmax: number, ymin: number, ymax: number, attr: DrawAttribute, str: string): void;
    setCursor(x: number, y: number): void;
    clearCursor(): void;
    prepare(width: number, height: number): void;
    flush(delta: boolean): void;
}
