import { Target, Turbo } from "@turbo/core";
export interface NodeConnectorConfig {
    readonly script: string;
    readonly nodePath?: string;
}
export default function node(config: NodeConnectorConfig, turbo: Turbo): Target;
