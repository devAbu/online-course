import { SessionId, EmitterBase, State, Turbo, Server, ServerEvents } from "@turbo/core";
export declare class SocketServer extends EmitterBase<ServerEvents> implements Server {
    private turbo;
    private lastClientId;
    private socketPath;
    private sessionId;
    private server;
    private connections;
    get numConnections(): number;
    constructor(turbo: Turbo, sessionId: SessionId);
    start(): void;
    stop(): void;
    broadcastState(state: State): void;
    broadcastQuit(): void;
    private createServer;
    private startAfterDelay;
    private handleSocket;
}
