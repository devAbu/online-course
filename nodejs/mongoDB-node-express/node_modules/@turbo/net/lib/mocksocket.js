"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function mockSocket() {
    const events = {};
    return {
        connect: jest.fn(),
        end: jest.fn(),
        write: jest.fn(),
        on: jest.fn().mockImplementation((name, callback) => {
            if (!events[name])
                events[name] = [];
            events[name].push(callback);
        }),
        fire(name, event) {
            if (events[name]) {
                for (const cb of events[name]) {
                    cb(event);
                }
            }
        },
    };
}
exports.mockSocket = mockSocket;
//# sourceMappingURL=mocksocket.js.map