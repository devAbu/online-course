/// <reference types="jest" />
import { ClientSocket, ClientSocketEvents } from "./baseclient";
declare type MockFire = {
    fire<N extends keyof ClientSocketEvents>(name: N, event: ClientSocketEvents[N]): void;
};
export declare function mockSocket(): jest.Mocked<ClientSocket> & MockFire;
export {};
