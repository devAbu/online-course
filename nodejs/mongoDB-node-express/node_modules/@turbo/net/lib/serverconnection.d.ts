import { SessionId, State, Turbo, ClientId, AnyMessage, AnyRequest, Response, ServerConnectionEvents, ServerConnection, ServerRequestType } from "@turbo/core";
import { BaseClient, ClientSocket } from "./baseclient";
export declare class SocketServerConnection extends BaseClient<ServerConnectionEvents> implements ServerConnection {
    readonly id: ClientId;
    constructor(turbo: Turbo, id: ClientId, sessionId: SessionId, socket: ClientSocket);
    sendState(state: State): void;
    sendQuit(): void;
    protected handleUnhandledMessage(msg: AnyMessage): void;
    protected handleUnhandledRequest(request: AnyRequest): Promise<Response<ServerRequestType>["payload"] | undefined>;
}
