import { State, Action } from "./state";
import { Server, Target, Turbo } from ".";
export interface Store {
    dispatch(action: Action): void;
    subscribe(cb: () => void): () => void;
    getState(): State;
}
export declare function makeStore(turbo: Turbo, server: Server, target: Target, state: State): Store;
