import { Server, TargetConnection, Turbo } from "..";
import { Channel } from "redux-saga";
export declare function serverFlow(turbo: Turbo, server: Server, targetConnectionChannel: Channel<TargetConnection | -1>): Generator<import("redux-saga/effects").ForkEffect<void>, void, unknown>;
