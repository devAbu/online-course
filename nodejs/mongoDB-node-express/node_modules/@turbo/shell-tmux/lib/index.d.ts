import { SessionId, Turbo, Layout, Shell } from "@turbo/core";
interface TmuxStartCommand {
    command: string;
    args: string[];
}
export declare function generateTmuxStartCommand(id: SessionId, layout: Layout, turbo: Turbo): TmuxStartCommand;
export default function tmux(options: {}, turbo: Turbo): Shell;
export {};
